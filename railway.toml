[build]
builder = "NIXPACKS"
buildCommand = "npm run build"

[deploy]
startCommand = "npm start"
releaseCommand = "npx prisma migrate deploy && npx tsx prisma/seed-all.ts && npx tsx prisma/seed-characters.ts"
